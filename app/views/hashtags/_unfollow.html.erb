<%= form_for(current_user.active_relationships.find_by(followed_id: @tag.id, followed_type: "Hashtag"),
             html: { method: :delete },
             remote: true) do |f| %>
    <%= hidden_field_tag :followed_type, "Hashtag" %>
  <%= f.submit "Unfollow", class: "btn" %>
<% end %>