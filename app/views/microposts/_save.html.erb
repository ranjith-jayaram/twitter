<%= form_for(current_user.active_relationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :followed_id, micropost.id %>
  <%= hidden_field_tag :followed_type, "Micropost" %></div>
	<%= image_submit_tag("save.jpeg", class: "save-button") %>
	<% end %> 
